{
  "name": "scorpions-security-rules",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Testes das Security Rules do Firestore e do Storage do Scorpions.",
  "scripts": {
    "test": "node --test --test-concurrency=1 test/firestore.test.mjs test/storage.test.mjs test/contract.test.mjs",
    "emulate": "firebase emulators:exec --project demo-scorpions --only firestore,storage,auth \"node --test --test-concurrency=1 test/firestore.test.mjs test/storage.test.mjs test/contract.test.mjs\"",
    "start": "firebase emulators:start --project demo-scorpions",
    "seed": "node seed.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0"
  }
}
